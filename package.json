{
  "name": "justjustin",
  "version": "1.0.0",
  "description": "justjustin.co.za - developer homepage",
  "scripts": {
    "build": "bash build.sh",
    "deploy": "dotenv -e .env -- bash -c 'pnpm build && lftp -c \"set ssl:verify-certificate no; open -u $FTP_USER,$FTP_PASSWORD $FTP_HOST; mirror -R dist/ ./\"'"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "clean-css-cli": "^5.6.3",
    "terser": "^5.31.0"
  },
  "private": true
}
